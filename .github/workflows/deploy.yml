name: Deploy Laravel Sail (Local)

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Ir al proyecto y actualizar código
        run: |
          set -e
          cd /home/hector/Escritorio/Despliegue/WebAutoGit
          git pull origin main

